
 <%= form_for(@rescue_complaint,:html=>{:name=>"f",:onsubmit=>"post_attend(event);"}) do |f| %>
   <%= f.hidden_field  :sighting_id ,:value => @sighting_id %>
    <%= f.submit "attend complaint", class: "btn btn-blue btn-lg" %>
<% end %> 